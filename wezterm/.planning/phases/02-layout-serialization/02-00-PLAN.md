---
phase: 02-layout-serialization
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - bin/test-phase2.sh
autonomous: true
requirements:
  - REQ-09

must_haves:
  truths:
    - "bin/test-phase2.sh exists, is executable, and provides test infrastructure for Phase 2 plans"
  artifacts:
    - path: "bin/test-phase2.sh"
      provides: "Test runner for Phase 2 validation"
      contains: "test_config_syntax"
  key_links:
    - from: "bin/test-phase2.sh"
      to: "lua/session/state.lua"
      via: "grep-based function existence checks"
      pattern: "function M\\."
---

<objective>
Create the Phase 2 test infrastructure script (`bin/test-phase2.sh`) so that all subsequent plans can reference it in their `<verify>` commands.

Purpose: Wave 0 requirement -- test scripts must exist before any plan references them in automated verification.

Output: Executable `bin/test-phase2.sh` with test functions for validating the state module.
</objective>

<execution_context>
@/Users/t026chirv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/t026chirv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Existing test script pattern from Phase 1 -->

From bin/test-phase1.sh:
```bash
#!/bin/bash
set -euo pipefail

# Color output helpers
green() { echo -e "\033[0;32m$*\033[0m"; }
red() { echo -e "\033[0;31m$*\033[0m"; }
yellow() { echo -e "\033[0;33m$*\033[0m"; }

# Test tracking: PASSED, FAILED, SKIPPED counters
# Parse arguments: --verbose flag, test name filter
# run_test() helper: runs test function, handles exit code 2 as SKIP
# Exit 0 if all pass (skips ok), exit 1 if any fail
# cd to script parent dir before running
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create bin/test-phase2.sh test infrastructure</name>
  <files>bin/test-phase2.sh</files>
  <action>
Create `bin/test-phase2.sh` following the exact pattern from `bin/test-phase1.sh`:

Shell script with:
- Shebang: `#!/bin/bash`
- `set -euo pipefail`
- Color-coded output (GREEN/RED/YELLOW) using same helper functions as Phase 1
- Test counter (PASSED/FAILED/SKIPPED)
- Support for `--verbose` flag and test name filtering (pass test name as arg)
- `run_test()` helper identical to Phase 1 (exit code 2 = SKIP)
- `cd "$(dirname "$0")/.."` at bottom before `main`

Tests to implement:

1. **test_config_syntax**: Run `wezterm --help > /dev/null 2>&1` -- verifies config loads without Lua syntax errors. (Same approach as Phase 1 test -- `wezterm show-config` does not exist.)

2. **test_state_module_exists**: Check `lua/session/state.lua` exists and is non-empty. If file does not exist yet, SKIP (exit code 2) with message "state.lua not yet created (Wave 1)".

3. **test_state_module_functions**: Use grep to verify the module exports expected functions:
   - `function M.uri_to_path`
   - `function M.capture_workspace`
   - `function M.save_current_workspace`
   - `function M.load_workspace`
   If `state.lua` does not exist, SKIP.

4. **test_sessions_dir**: Verify `sessions/` directory exists.

5. **test_json_structure**: If any `.json` file exists in `sessions/`, validate it contains expected top-level keys (`version`, `workspace`, `last_saved`, `tabs`) using grep. If no session file exists yet, SKIP (exit code 2).

6. **test_init_exposes_state**: Grep `lua/session/init.lua` for `M.state` to confirm the state module is wired. If not found, SKIP with message "state module not yet wired (Wave 1)".

Each test function outputs PASS/FAIL/SKIP with test name. Script exits 0 if all pass (skips ok), 1 if any fail.

Make the script executable: `chmod +x bin/test-phase2.sh`.

IMPORTANT: Tests 2, 3, and 6 should SKIP (not FAIL) when the artifacts they check don't exist yet, since this Wave 0 plan runs before the artifacts are created. This allows the test script to be created first, then later plans create the artifacts that make the tests pass.

Do NOT:
- Try to run Lua code directly (WezTerm Lua runtime is not accessible from shell)
- Depend on a running mux daemon for basic tests
- Use `wezterm show-config` (does not exist -- Phase 1 learned this)
  </action>
  <verify>
    <automated>bash bin/test-phase2.sh --verbose</automated>
  </verify>
  <done>
- `bin/test-phase2.sh` exists and is executable
- Running it exits 0 (all tests pass or skip -- artifacts not yet created will skip)
- Script follows identical pattern to `bin/test-phase1.sh`
- test_config_syntax passes (wezterm config is valid)
- test_sessions_dir passes (directory exists from Phase 1)
- Other tests skip gracefully until Wave 1 creates the artifacts
  </done>
</task>

</tasks>

<verification>
1. `test -x bin/test-phase2.sh` exits 0 (file exists and is executable)
2. `bash bin/test-phase2.sh` exits 0 (no failures, skips are acceptable)
3. `bash bin/test-phase2.sh --verbose` shows test names with PASS/SKIP status
</verification>

<success_criteria>
- bin/test-phase2.sh exists, is executable, runs without errors
- Test infrastructure is ready for Wave 1+ plans to reference in verify commands
- Tests that check not-yet-created artifacts skip gracefully
</success_criteria>

<output>
After completion, create `.planning/phases/02-layout-serialization/02-00-SUMMARY.md`
</output>
