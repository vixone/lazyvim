# Panel kitty config — used by quick-access-terminal
# Inherits all main settings, but disables cmd+f so the panel doesn't
# toggle itself closed when it receives focus while Cmd is still held.
include kitty.conf

# Prevent double-toggle: panel has no cmd+f binding
map cmd+f no_op
